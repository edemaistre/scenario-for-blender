# Scenario for Blender (project pointer)

Read `README.md` first, then `docs/superpowers/specs/2026-08-28-scenario-for-blender-design.md` (the approved v1 design).

**State (2026-08-29, night):** v0.9.0 on `main`, first release-candidate pass: continuous segmented tabs everywhere (`panels.equal_segments`: align row + even split chain), UX harmonisation (spacing, wording, `docs/UI_STYLE.md`), and a 16-finding audited production-readiness sweep (MCP estimate_cost/wait_for_job off the HTTP thread, corrupt-cache guard, composer breaker deferred, MCP Python OFF by default, dead code removed, lane->kind map consolidated in core, log level wired). 198 unit + 123 headless tests. v0.8.3: tabs/buttons justified full width with icon glued to the label centred per cell (`panels.draw_enum_tabs` and `model_picker._centered_cells`: a column per value expands, a CENTER row inside centres the content), prompt box with the prompt in the field and no wrapped duplication + draggable height (`prompt_rows`), prompt tools labelled New/Rewrite/Translate so they fill the width (icon-only Blender buttons never stretch), Prompt Spark click regression fixed (0.8.2 centred+scaled row swallowed clicks), Edit-prompt dialog removed, Render Image/Video "Rendering Style" collapsible box holding look + style images + first frame (Last Frame removed), composer minus button a full tab-height cell. v0.8.2: UI polish (compact centred icon tabs via `row.alignment='CENTER'`, prompt box, redrawn prompt icons, picker spacing, Jobs count label, cloud entries drawn as session entries). v0.8.1: SPZ Gaussian splat reader (`core/scene/spz.py`, worlds from Marble/HY World load as coloured point clouds; `.bin` splats recognised by content), image actions (View image fitted, Convert to 3D, Use as reference, Remove background), Reload parameters, Settings dialog from the composer (`scenario.quick_settings`), composer height fitted and grip hidden. v0.8.0: movable/resizable composer (prefs-persisted), camera path built from editable markers (Place markers, Closed loop, Clear path, confirmations), Delete on 3D results via a job tag on objects, camera clips captured through the viewport shading (were always clay), duration/clip sync for numeric ranges (H3), Prompt Spark asset-id answers resolved + LLM fallback, Scenario icons on the prompt tools, temporary status messages, operator descriptions. Previous: v0.7.0: Scenario taxonomy in the model picker (modality tabs + category chips, LoRAs excluded everywhere via `catalog.is_lora`), Audio lane (sequencer strips), Edit 3D folded under the 3D tab as an Edit mode, prompt tools (Spark / Rewrite / Translate, `prompt_tools.py` + `core/api/llm.py`), 20-move camera library (`shot_plan.PRESETS`, closed orbits), Generations entries collapsible with asset id copy + Details dialog, asset ids in file names, GPT Image 2 default for Render Image. Previous: v0.6.0: Render Image / Render Video lanes with Prompt Spark (`POST /generate/prompt`) and role-explicit prompts (`core/scene/render_prompt.py`), camera path planner (`shot_planner.py`, markers + presets + MCP `camera_path`), Edit 3D lane (mesh export to GLB, result next to the source), model picker dialog, composer text selection, four panels (Scenario / Jobs / Generations / Agents), dated output folders. Render-to-real archived in `archive/0.5.x/`. Versioning rule applied: `versions/` for docs, `archive/` for retired code, one zip per version in `dist/`. Previous state: v0.5.2 (0.5.0 + the 3D import fixes + retrying downloads). User docs: `docs/USER_GUIDE.md` and `docs/user-guide.html` (published artifact https://claude.ai/code/artifact/6d24a64f-5a31-4cd8-9fc9-7decd743e154, republish from this file path to keep the URL), `CHANGELOG.md` in Keep-a-Changelog form (previous form in `versions/`). P0 to P4 delivered: Image, Materials via Patina, 3D import at cursor, Generations history, Video lane with captures, Render-to-real, local MCP server (16 tools), floating gpu/blf composer, extension repository build; 76 unit + 54 headless tests green; four real smokes OK; 29 of 31 review findings fixed (open ones in BUGS.md). Next: v2 backlog in the spec section 4 (skyboxes, mesh utilities, rigging/motion, LoRAs, Workflows, OAuth), and the BUGS.md items. Research notes in `research/` (clean-room behavioural specs of [redacted] 1.5.47 + Scenario API/model maps + live probes). Dev credentials in git-ignored `.env.local` (workspace "Blender Plugin Tests", ~$30 test cap, never print them).

Hard rules for this folder:
- Clean-room: never copy code from the [redacted] source in `~/Developer/scratch/2026-08-28 [redacted] Blender Plugin 1.5.47/`; behaviour only.
- Pure Python, no wheels; `bpy` on the main thread only; GPL-3.0-or-later SPDX header on every source file; no em dashes anywhere.
- `dryRun` is a QUERY param on REST (body flag runs a paid job).
- Version before overwrite (`versions/`), archive instead of delete (`archive/`), one commit per phase, tests + headless checks + GUI screenshots before saying "delivered".

**Resume this work:** `claude --resume f634b620-910d-4c24-aeef-bc9232faeca7` (2026-08-28, research + design + P0)
