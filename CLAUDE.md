# Scenario for Blender (project pointer)

Read `README.md` first, then `docs/superpowers/specs/2026-08-28-scenario-for-blender-design.md` (the approved v1 design).

**State (2026-08-29, night):** v0.9.9 on `main` (0.9.8 + BLOCKOUT-EMPTY BUG fix. "room" -> "blockout came back empty". ROOT: `llm.text_from_job` read the LLM answer from the text asset's `metadata.preview`, which the API CAPS at ~1024 chars; a blockout plan is 2-9 KB, so the JSON was cut off mid-element and `json.loads` failed -> parse_plan []. FIX: the text asset carries `hasFullPreview` (bool) + `charCount`; when `hasFullPreview` is False, fetch the FULL content from `asset['url']` via new `assets.fetch_url_text(url)` (raw urllib GET, decode utf-8) instead of the capped preview. DEFENSE-IN-DEPTH: `blockout._extract_json_array` now, if the whole `[...]` fails to parse, calls `_recover_objects` (balanced-brace scan honouring strings/escapes) to salvage the complete `{...}` elements before the cut. Verified LIVE: real LLM "room" -> 2693 chars -> 17 elements; GUI design of an artist's studio -> 15 elements built (`0.9.9-blockout-live.png`). This capped-preview gotcha affects ANY long Scenario-LLM output (Prompt Spark rewrites are short so were fine). 221 unit + 140 headless green.). Prior: v0.9.8 on `main` (0.9.7 + PROMPT-TO-BLOCKOUT as its OWN TAB, a real scene-design system (user: 0.9.7's box-dropper was "WAY too basic"). New lane tab `blockout` (added to `props.LANE_ITEMS` + a 4th tab row `("blockout",)`, routed in `SCENARIO_PT_main.draw` BEFORE the catalog-loaded gate since it needs no model). Scene state in `scene.scenario_blockout` (`ScenarioBlockoutProps`: prompt, scene_type enum, scale enum, refine, plan_json). Advanced model in `core/scene/blockout.py`: elements now have `category` (floor/wall/structure/prop/furniture/vegetation/vehicle/water/light -> greybox COLOUR + sub-collection), `primitive` (box/cylinder/plane/wedge/cone/sphere), `rotation` yaw, `group`. `instruction(prompt,scene_type,scale,previous)` (previous=refine), `parse_plan` (was parse_blockout; clamps, bad enums fall back, cap MAX_ELEMENTS=200), `plan_summary` (counts by category+group). Builder `blender/blockout.py`: shared unit mesh per primitive (box/wedge from_pydata; cylinder/cone/sphere via `bmesh.ops.create_cone`/`create_uvsphere`, pump-safe no ops), `_mesh_for` guards stale-ref with try/except ReferenceError; groups into sub-collections under "Blockout" (group=="Blockout" -> root directly, else collision); `obj.color`=category colour + `_colour_the_viewport` sets every VIEW_3D `shading.color_type='OBJECT'`. Flow: design (async LLM `_design` -> `blockout_plan` event -> `on_blockout_plan` stores plan_json + builds), refine (re-design with previous plan), rebuild (sync from plan_json), clear. Operators `scenario.blockout_{design,refine,build,clear}`. Old 3D-tab `scenario.blockout` button REMOVED. `draw_blockout_tab` shows prompt+type+scale, Design, plan summary (element/group counts + category legend), Rebuild/Clear, Refine. 219 unit + 140 headless green; GUI `0.9.8-blockout-tab.png` (tower+spire+tree+well+walls+stalls, green/blue category colours, tab + summary + refine visible).). Prior: v0.9.7 on `main` (0.9.6 + three feature asks. (A) Two new MCP tools in `mcp/tools_blender.py` inspired by Blender's official `blender_mcp` (projects.blender.org/lab/blender_mcp, GPL-3.0-or-later, written FRESH not copied): `blender_api_help(path)` introspects a bpy operator/type/collection (docstring + RNA properties/members) so an agent checks the API before execute_python; `datablocks_summary()` counts objects/meshes/materials/etc. blender_mcp itself is a minimal MCP-server+addon-over-TCP that overlaps ~80% with our own MCP (Python exec, scene, screenshots, camera path, generate), so we did NOT fork it. (B) **Blockout from prompt** (`SCENARIO_OT_blockout`, 3D tab button): the Scenario LLM turns a scene description into grey boxes in a "Blockout" collection. Pure parse/sanitize in `core/scene/blockout.py` (`parse_blockout` tolerates code fences/prose, clamps sizes to 0.05-200 m and coords to +/-500, caps 80 boxes); async via `manager._spawn` + a `blockout` event -> `blockout.on_blockout_event` builds cubes (one shared unit-cube mesh, per-object scale). (C) **Camera-path holds/pauses**: `shot_plan.plan_from_text` now parses "hold 2 s"/"pause"/"stay for 3 s" (cut out before duration parsing) into a `hold` value applied to the ARRIVAL marker; per-marker Hold (s) field in the panel when a Shot marker is active (`active_marker` helper, draws the PROP_HOLD custom prop). The hold engine (`Waypoint.hold`, `frame_schedule`) already existed. 213 unit + 137 headless green. GUI verified `0.9.7-blockout.png` (grey boxes + Blockout button).). Prior: v0.9.6 on `main` (0.9.5 + enum-cache bug fix. Minimax H3's Resolution/Aspect Ratio dropdowns showed "Loading..."/"enum_value" with empty fields. ROOT: enum param choices live in the in-memory `runtime.state.enum_cache` (set by `sync_params`), which a saved .blend does NOT carry; on reopen the params persist but the cache is empty, so `props._param_items` returned the "Loading..." fallback until a re-sync. GENERAL bug: hit every enum (allowed_values) setting on every model after a file reload, H3 just has two prominent ones. FIX: `_param_items` now rebuilds the choices from `generation.schema_for(model_id)` on a cache miss (draw-safe: reads the record, writes only the plain enum-cache dict). Confirmed via probe + headless test (clear cache -> rebuild). 208 unit + 130 headless green. NOTE: this only manifests after a .blend reload, so a fresh-session screenshot can't show it; verified by test/probe, not a GUI shot.). Prior: v0.9.5 on `main` (0.9.4 + a batch of fixes. (1) **Mesh auto-attach**: the selected scene mesh now feeds a model's kind:3d character input in ANY lane automatically (was Edit-lane only + a click that got missed, so Uthana Text to Motion ran without the mesh - verified via asset_DS1Pm.../job input had no characterFile). `generation.build_request` attaches the selection as a MESH capture unless an explicit ref exists; `panels.draw_references` pins "Selected mesh: <name>"; `addable_sources_for("3d")` is now just Upload (override). Deselect = text-only. (2) **Reload parameters** fixed for edit3d/Generate: reload set the lane but not the sub-selector (edit3d_task / three_d_mode) that GATES the model enum, so a Meshy 7 Retexture reloaded onto the wrong model. Now sets the task/mode that lists the model FIRST, and skips restoring the kind:3d mesh as a stale asset (comes from selection). (3) **Camera-path Plan** clarified: deterministic keyword reader (NOT AI, user chose "keep deterministic, clearer"); `plan_from_text` now picks the FIRST move named in the text (earliest match position, specificity breaks ties) so "dolly in and then orbit"->dolly_in predictably; placeholder/tooltip updated. (4) **Trash button** on the prompt (`SCENARIO_OT_prompt_clear`, greyed when empty). (5) **Collapse all/Expand all** for generations (`SCENARIO_OT_collapse_results`). (6) **Full error display** (web-app style): `SCENARIO_OT_error_details` with a dynamic `description()` = the full error (hover tooltip incl. Error ID) + a popup with the wrapped message and Copy; red alert row; header error button visible when collapsed. Native popups still can't drag-resize (user said "leave for now"). 208 unit + 129 headless green against the built+installed zip. GUI verified: `0.9.5-uthana-auto-mesh.png` (pinned Selected mesh + trash icon), `0.9.5-error-collapse.png` (Collapse all + red error + (i)). Prior: v0.9.4 on `main` (0.9.3 + 3D mesh-input fix. Reported: "Uthana Text to Motion ignored my selected mesh." ROOT: the plugin only auto-attached the selected scene mesh in the 3D **Edit** lane; models that take a mesh but live elsewhere (Uthana Text to Motion is `txt23d` with an optional `characterFile` kind:3d "Character 3D model", in 3D **Generate/Text**) never received it, and the reference "Add" menu offered the same sources for every input (a viewport still could be attached to a 3D character input). FIX: `props.addable_sources_for(kind)` drives per-kind add options (3D input -> **Selected mesh** + **Upload**, never an image capture; image -> stills; video -> clips); `draw_references` renders those; `build_request` routes a `MESH`-source reference to a GLB-export capture in any lane; the add-reference operator's `source` enum widened to all `REFERENCE_SOURCES`. Also `_parse_required` now handles `required:{ifNotDefined:{...}}` (Cartwheel: a 3D mesh OR a reference image) -> a `one_of` group. Reviewed ALL 74 public 3D-capable models: 45 no mesh input (ok), 27 mesh reachable now, 2 orphaned `video23d` (Uthana/Cartwheel Video-to-Motion, no lane - pre-existing gap). ALSO caught: the isolated headless dir had been running STALE installed code, so the 0.9.2/0.9.3 headless "green" runs did NOT exercise those changes; re-installed 0.9.4 into the isolated dir and found 2 render_video duration tests still asserting the pre-0.9.2 clip->duration direction - updated them to the 0.9.2 contract (model duration is the source; base Video lane keeps clip->duration, covered by test_video_lane). 208 unit + 125 headless green (against the actually-installed build), GUI verified (`0.9.4-uthana-mesh.png` shows Selected mesh + Upload on Uthana's Character 3D model). Prior: v0.9.3 on `main` (0.9.2 + payload audit: **Rodin Bang bug fixed** and a full model-payload audit. Bang (Retexture/Parts) marked its reference `image` as `required:true` while its own description makes it an either/or with the prompt ("...if no prompt is provided"), so the plugin blocked the valid prompt-only path with "Reference Image is required". Fix in `core/schema/params.py`: a required file whose OWN description reads conditional (`if no`/`if not`/`when no`/`unless`/`if none`) AND that sits next to a prompt is relaxed to optional, and the pair becomes a `Schema.one_of` group; `validate(specs, body, one_of)` requires at least one and shows "Provide one of: Reference Image or Texture Prompt" only when neither is given. General + schema-driven, covers any future model phrased the same way. Also fixed 3 dead curated ids (seedream-5-0→-lite, google-veo-3-1→veo3-1, kling-3-0→kling-v3-omni-video). New `tools/audit_payloads.py` fetches every surfaced model's live schema (68) and checks required flags / either-or / mesh detection / dropped params; report in `docs/MODEL_PAYLOAD_AUDIT.md`; 68/68 clean, the one note left (Trellis 2 Retexture needs mesh+image) is correct by design. 204 unit + 123 headless green, GUI screenshot verified (`0.9.3-bang-retexture.png`).). Prior: v0.9.2 on `main` (0.9.1 + section/duration batch: **Model** is now a boxed section with an icon header like Clip to render / Camera path; **Settings** header carries the `PREFERENCES` icon; **Rendering Style** is a plain always-open section (`BRUSH_DATA`, chevron/collapse removed). Duration direction flipped for Render Video: the model's `Duration (cost)` in Settings is the source, and with Match timeline on the camera-path `Duration (s)` is read-only and follows it via `generation.sync_shot_duration` (called from `props._on_param_update` on the duration param and from `_on_match_timeline`, never during draw), with a plain note under the planner "The clip is captured at X s to match the video duration". `apply_match_timeline` is now guarded to `lane == "video"` so only the base Video lane still derives the model duration from the timeline; the backwards "Clip padded/trimmed for the model" note was removed from Render Video. 198 unit + 123 headless green, GUI screenshot verified.). Prior: v0.9.1 on `main` (0.9.0 + UI batch: tabs = full-width continuous cells filling each row via a split chain and prop_enum, NOT forced equal across rows and NOT content-sized, icon/text left to Blender's enum; Rendering Style header harmonised; boolean settings single checkbox (never write item.enabled during draw - set enabled at collect_values); Reference Audio restored; flat references boxed=False; camera-path placeholder; Clear path says Clear; verbose inline labels removed for hover tooltips). v0.9.0, first release-candidate pass: continuous segmented tabs everywhere (`panels.equal_segments`: align row + even split chain), UX harmonisation (spacing, wording, `docs/UI_STYLE.md`), and a 16-finding audited production-readiness sweep (MCP estimate_cost/wait_for_job off the HTTP thread, corrupt-cache guard, composer breaker deferred, MCP Python OFF by default, dead code removed, lane->kind map consolidated in core, log level wired). 198 unit + 123 headless tests. v0.8.3: tabs/buttons justified full width with icon glued to the label centred per cell (`panels.draw_enum_tabs` and `model_picker._centered_cells`: a column per value expands, a CENTER row inside centres the content), prompt box with the prompt in the field and no wrapped duplication + draggable height (`prompt_rows`), prompt tools labelled New/Rewrite/Translate so they fill the width (icon-only Blender buttons never stretch), Prompt Spark click regression fixed (0.8.2 centred+scaled row swallowed clicks), Edit-prompt dialog removed, Render Image/Video "Rendering Style" collapsible box holding look + style images + first frame (Last Frame removed), composer minus button a full tab-height cell. v0.8.2: UI polish (compact centred icon tabs via `row.alignment='CENTER'`, prompt box, redrawn prompt icons, picker spacing, Jobs count label, cloud entries drawn as session entries). v0.8.1: SPZ Gaussian splat reader (`core/scene/spz.py`, worlds from Marble/HY World load as coloured point clouds; `.bin` splats recognised by content), image actions (View image fitted, Convert to 3D, Use as reference, Remove background), Reload parameters, Settings dialog from the composer (`scenario.quick_settings`), composer height fitted and grip hidden. v0.8.0: movable/resizable composer (prefs-persisted), camera path built from editable markers (Place markers, Closed loop, Clear path, confirmations), Delete on 3D results via a job tag on objects, camera clips captured through the viewport shading (were always clay), duration/clip sync for numeric ranges (H3), Prompt Spark asset-id answers resolved + LLM fallback, Scenario icons on the prompt tools, temporary status messages, operator descriptions. Previous: v0.7.0: Scenario taxonomy in the model picker (modality tabs + category chips, LoRAs excluded everywhere via `catalog.is_lora`), Audio lane (sequencer strips), Edit 3D folded under the 3D tab as an Edit mode, prompt tools (Spark / Rewrite / Translate, `prompt_tools.py` + `core/api/llm.py`), 20-move camera library (`shot_plan.PRESETS`, closed orbits), Generations entries collapsible with asset id copy + Details dialog, asset ids in file names, GPT Image 2 default for Render Image. Previous: v0.6.0: Render Image / Render Video lanes with Prompt Spark (`POST /generate/prompt`) and role-explicit prompts (`core/scene/render_prompt.py`), camera path planner (`shot_planner.py`, markers + presets + MCP `camera_path`), Edit 3D lane (mesh export to GLB, result next to the source), model picker dialog, composer text selection, four panels (Scenario / Jobs / Generations / Agents), dated output folders. Render-to-real archived in `archive/0.5.x/`. Versioning rule applied: `versions/` for docs, `archive/` for retired code, one zip per version in `dist/`. Previous state: v0.5.2 (0.5.0 + the 3D import fixes + retrying downloads). User docs: `docs/USER_GUIDE.md` and `docs/user-guide.html` (published artifact https://claude.ai/code/artifact/6d24a64f-5a31-4cd8-9fc9-7decd743e154, republish from this file path to keep the URL), `CHANGELOG.md` in Keep-a-Changelog form (previous form in `versions/`). P0 to P4 delivered: Image, Materials via Patina, 3D import at cursor, Generations history, Video lane with captures, Render-to-real, local MCP server (16 tools), floating gpu/blf composer, extension repository build; 76 unit + 54 headless tests green; four real smokes OK; 29 of 31 review findings fixed (open ones in BUGS.md). Next: v2 backlog in the spec section 4 (skyboxes, mesh utilities, rigging/motion, LoRAs, Workflows, OAuth), and the BUGS.md items. Dev credentials in git-ignored `.env.local` (workspace "Blender Plugin Tests", ~$30 test cap, never print them).

Hard rules for this folder:
- All code is original: never copy code from any third-party reference implementation; write our own from behaviour only.
- Pure Python, no wheels; `bpy` on the main thread only; GPL-3.0-or-later SPDX header on every source file; no em dashes anywhere.
- `dryRun` is a QUERY param on REST (body flag runs a paid job).
- Version before overwrite (`versions/`), archive instead of delete (`archive/`), one commit per phase, tests + headless checks + GUI screenshots before saying "delivered".

**Resume this work:** `claude --resume f634b620-910d-4c24-aeef-bc9232faeca7` (2026-08-28, research + design + P0)
